{
  "scripts": {
    "test_frontend": "cd client/TastyBite && npm i && cd .. && npx cypress run && cd ..",
    "test_backend": "cd server && npm i && npm run test && cd .."
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "jest-cucumber": "^3.0.1",
    "typescript": "^5.3.2"
  }
}
